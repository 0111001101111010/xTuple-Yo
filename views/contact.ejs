<!DOCTYPE html>

<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="stylesheets/csshake.min.css">
    <link rel="stylesheet" href="stylesheets/pop.css">
    <link rel="stylesheet" href="http://yui.yahooapis.com/pure/0.5.0/pure-min.css">
    <link rel="stylesheet" href="stylesheets/styles.css">
    <link href='http://fonts.googleapis.com/css?family=Montserrat' rel='stylesheet' type='text/css'>
    <script src="http://code.jquery.com/jquery-1.10.0.min.js"></script>
<!--     <script type="text/javascript"
src="//static.twilio.com/libs/twiliojs/1.2/twilio.min.js"></script>
 -->    <title><%= title %></title>
  </head>
  <body>
    <a class="modalLink" href="#" name="_csrf" hidden></a>
    <div class="overlay" id="overlay"></div>
  <form action="#">
    <span class="modal" id="modal">
      <input type="text" id="myName" placeholder="YOUR NAME">
      <input type="text" id="myMsg" placeholder="YOUR MESSAGE">
      <button class="button-secondary pure-button" id="myClick">Submit</button>
    </span>
  </form>
  <span class="pure-g">
    <span class="pure-u-1" id="title"><p>xTuple-Yo!</p></span>
    <% for(var i = 0; i < contacts.length; i++) { %>
      <% if((contacts[i].phone !== null) && (contacts[i].firstName !== 'null') && (contacts[i].lastName !== null)) {%>
        <% if(i%2!==0) {%>
      <div class="pure-u-1 orange" id="tap">
      <% }else {%>
      <div class="pure-u-1 blue" id="tap">
      <% } %>
        <p class="name"><%= contacts[i].firstName + " " + contacts[i].lastName %>
        <p class="phone" name="_csrf" hidden><%= contacts[i].phone %></p>
      </div>
      <% } %>
    <% } %>
  </span>
  </body>
  <script src="js/client.js"></script>
  <script src="js/jquery.modal.min.js"></script>
</html>
